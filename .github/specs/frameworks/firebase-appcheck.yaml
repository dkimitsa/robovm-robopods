name: firebase-appcheck
moduleFolder: firebase/ios-appcheck
artifactSource:
  - check if folder exists in ./.robopods-wip/Firebase/FirebaseAppCheck/FirebaseAppCheck.xcframework/, stop and report error if not
downloadLocation: ./.robopods-wip/Firebase
