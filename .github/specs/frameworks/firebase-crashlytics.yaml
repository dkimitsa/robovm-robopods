name: firebase-crashlytics
moduleFolder: firebase/ios-crashlytics
artifactSource:
  - check if folder exists in ./.robopods-wip/Firebase/FirebaseCrashlytics/FirebaseCrashlytics.xcframework/, stop and report error if not
downloadLocation: ./.robopods-wip/Firebase
