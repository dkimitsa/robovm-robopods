name: firebase-firestore
moduleFolder: firebase/ios-firestore
artifactSource:
  - check if folder exists in ./.robopods-wip/Firebase/FirebaseFirestore/FirebaseFirestore.xcframework/, stop and report error if not
downloadLocation: ./.robopods-wip/Firebase
